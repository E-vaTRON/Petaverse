<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Petaverse.Components.PetWikiCard">
    <VerticalStackLayout>
        <Label 
            x:Name="TestLabel"
            Text="{Binding BreedInfo.BreedName, Mode=TwoWay}"
            TextColor="Coral"
            VerticalOptions="Center" 
            HorizontalOptions="Center" 
            HeightRequest="30"/>
        <Label Text="This is the card"/>
    </VerticalStackLayout>
</ContentView>
